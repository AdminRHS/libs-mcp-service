{
  "name": "libs-mcp-service",
  "version": "1.0.0",
  "description": "Minimal MCP service for integration with external platforms",
  "main": "libs-mcp-service.js",
  "bin": {
    "libs-mcp-service": "./libs-mcp-service.js"
  },
  "scripts": {
    "build": "npx esbuild index.js --bundle --platform=node --target=node18 --outfile=libs-mcp-service.js --external:child_process --external:readline --external:process --external:fs --external:path --external:url",
    "start": "node libs-mcp-service.js",
    "dev": "node index.js"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "api",
    "integration",
    "mcp-service",
    "mcp-server",
    "mcp-client",
    "libs"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.2"
  },
  "devDependencies": {
    "esbuild": "^0.19.8"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/AdminRHS/libs-mcp-service.git"
  }
}
